/**
 * Domain health slice — API response shape is shared via @lilo-hub/contracts (HealthResponse).
 * Keep domain-specific invariants here as the guia grows.
 */
export type { HealthResponse } from '@lilo-hub/contracts';
